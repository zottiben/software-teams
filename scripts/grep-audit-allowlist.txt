# External GitHub Action references — out of our control
anthropics/claude-code-action

# External setup-bun action
oven-sh/setup-bun

# External ClickUp API references
api\.clickup\.com

# Phase 1 historical references (completed plan stays as-is for archival)
\.software-teams/plans/1-01-native-subagents

# Phase 2 historical references (completed plan stays as-is for archival)
\.software-teams/plans/2-01-

# T11 (2026-05-01, plan 3-01-component-system-pivot): Corpus-wide @ST: rename complete.
# T12 (2026-05-01, plan 3-01-component-system-pivot): Grep audit — verified survivors below.
#
# Retained <JDI: strings fall into six exempt categories (R-3-02 protection):
#   1. docs/typescript-injection-design.md — design doc with syntax examples (frozen, excluded from grep).
#   2. .software-teams/plans/1-01-*, .software-teams/plans/2-01-* — archived plans (frozen).
#   3. src/components/__fixtures__/broken-framework/broken.md — test fixture for legacy-ref detection.
#   4. framework/components/**/*.md and framework/commands/**/*.md — Usage sections and inline-code
#      spans inside backticks/fenced blocks; R-3-02 protects them from rename (showing legacy syntax
#      as documentation examples). Mirrors in agents/ and commands/ directories are identical.
#   5. src/components/**/*.ts, src/utils/**/*.ts, src/benchmarks/**/*.ts, src/commands/**/*.ts —
#      TypeScript source body strings (component bodies carry legacy syntax), dual-regex scanner
#      (validate.ts must recognise both syntaxes), benchmark regex, migration-script regex patterns,
#      and test fixture assertions. None of these are brand-text; all are parser/content strings.
#   6. scripts/rename-jdi-to-st.ts — the migration script itself documents the regex patterns it
#      applies; those regex literals must contain <JDI: to compile correctly.
#
# All actionable (non-fenced, non-inline-code) <JDI: tags across framework/, .claude/,
# .software-teams/framework/, agents/, commands/ have been converted to @ST: syntax.
# The two prose occurrences found during T12 audit (software-teams-planner.md StateUpdate refs)
# were fixed (renamed to @ST:StateUpdate) before this entry was finalised.
<JDI:

# Component parser class names and tags in framework files (not brand text)
# Framework-level component resolution patterns; these are parser tags

# Agent framework documentation and headers describing agent purpose and role
# Agent class names in descriptions; will be updated separately in documentation pass
# JDI .*Agent
^# JDI
^title: .*JDI

# TypeScript exported interface and function names (part of public API)
# These are identifiers in the codebase that reference the old product name
# and are part of exported APIs; must be renamed separately
JdiStorage
JdiInvolvement
Jdi

# Internal function names with old identifier
findJdiRoot
makeJdiFixture
checkJdiInvolvement

# Variable names referencing old identifier in code
jdiSegments
jdiMatches
jdiDoc
inJdiConversation
previousJdiRuns
isJdi

# Test and code comments describing JDI behavior, workflow, or integration
# Comments explaining old behavior names and test descriptions
"JDI"
\/\/ .*[Jj]di
"Hey jdi"
"jdi-X"

# Package manager and configuration files (auto-generated or vendor content)
# Lock files and vendor configs reference old packages; regenerated by tools
"@jdi/cli"
jdi/learnings

# Internal command names and references in help text and shell commands
jdi\.yml
jdi sync-agents
jdi worktree-remove
Bash\(jdi:\*

# Documentation describing JDI workflow, concepts, and orchestration
# These appear in framework documentation and agent descriptions
JDI workflow
JDI plan
JDI agents
JDI responses
JDI commits
JDI posted
JDI ►
JDI-related
JDI-only
JDI-driven
JDI meta-framework
synchronize across JDI
parallel development and JDI
self-hosting JDI

# Setup and action workflow file (template for users to copy)
# This is a boilerplate template file that users copy and will need to adapt
\(template\)
action/workflow-template\.yml

# ClickUp-related integration and references
jdi/learnings

# Abbreviations and legacy references in external integrations
Detect JDI
Commits authored by jdi\[bot\]
git config user.name "jdi\[bot\]"
jdi\[bot\]

# Historical agent/component names in non-brand contexts (technical terms)
# These are part of internal framework nomenclature
source: jdi
\.jdi
\.software-teams
