name: "{project_name}"
summary: "{one_liner_description}"

core_value: "{the_one_thing_that_must_work}"

background: "{why_this_project_exists}"

constraints:
  - "{constraint_1}"
  - "{constraint_2}"

tech_stack:
  backend: null    # e.g. php-laravel, python-django, go, dotnet, ruby-rails, node-express
  frontend: null   # e.g. react-typescript, vue-typescript, svelte, angular, nextjs
  devops: null     # e.g. docker-k8s, serverless, heroku (optional — auto-detected from infra files)

success_criteria:
  - "{measurable_criterion_1}"
  - "{measurable_criterion_2}"

last_updated: "{date}"
